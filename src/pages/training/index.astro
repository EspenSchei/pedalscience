---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Button from "../../components/Button.astro";

const title = "Cycling Training Guides | PedalScience";
const description =
    "Evidence-based training guides for amateur cyclists: FTP, sweet spot, VO2max, pacing, and endurance.";

// Get all markdown articles in this folder
const rawArticles = await Astro.glob("./*.md");

const WORDS_PER_MINUTE = 220;

// Priority (low number = show first)
const getPriority = (url: string) => {
    switch (url) {
        case "/training/4-watt-per-kg":
            return 1;
        case "/training/sweet-spot-progression":
            return 2;
        default:
            return 99;
    }
};

// Difficulty order for sorting
const difficultyOrder = (difficulty?: string) => {
    switch ((difficulty ?? "").toLowerCase()) {
        case "beginner":
            return 1;
        case "intermediate":
            return 2;
        case "advanced":
            return 3;
        default:
            return 2;
    }
};

// Color schemes based on difficulty
const getDifficultyColor = (difficulty?: string) => {
    switch ((difficulty ?? "").toLowerCase()) {
        case "beginner":
            return { bg: "from-emerald-500 to-emerald-600", shadow: "shadow-emerald-500/30", border: "hover:border-emerald-200", text: "text-emerald-600", light: "from-emerald-100 to-emerald-200" };
        case "intermediate":
            return { bg: "from-sky-500 to-sky-600", shadow: "shadow-sky-500/30", border: "hover:border-sky-200", text: "text-sky-600", light: "from-sky-100 to-sky-200" };
        case "advanced":
            return { bg: "from-orange-500 to-orange-600", shadow: "shadow-orange-500/30", border: "hover:border-orange-200", text: "text-orange-600", light: "from-orange-100 to-orange-200" };
        default:
            return { bg: "from-slate-500 to-slate-600", shadow: "shadow-slate-500/30", border: "hover:border-slate-300", text: "text-slate-600", light: "from-slate-100 to-slate-200" };
    }
};

// Build article meta
const articles = await Promise.all(
    rawArticles.map(async (mod) => {
        const frontmatter: any = mod.frontmatter ?? {};
        const html = (await mod.compiledContent?.()) ?? "";
        const text = html.replace(/<[^>]+>/g, " ");
        const words = text.split(/\s+/).filter(Boolean).length;
        const readingMinutes = words
            ? Math.max(1, Math.round(words / WORDS_PER_MINUTE))
            : undefined;

        const tags: string[] = frontmatter.tags ?? [];
        const difficulty: string | undefined = frontmatter.difficulty;
        const updated: string | undefined = frontmatter.updated;

        let updatedLabel: string | undefined = undefined;
        if (updated) {
            const d = new Date(updated);
            if (!Number.isNaN(d.getTime())) {
                updatedLabel = d.toLocaleDateString("en-US", {
                    day: "numeric",
                    month: "long",
                    year: "numeric",
                });
            }
        }

        const colors = getDifficultyColor(difficulty);

        return {
            url: mod.url,
            title: frontmatter.title ?? "Untitled",
            description: frontmatter.description ?? "Read the article",
            tags,
            difficulty,
            updated,
            updatedLabel,
            readingMinutes,
            colors,
            priority: getPriority(mod.url),
        };
    })
);

// Sort articles
articles.sort((a, b) => {
    if (a.priority !== b.priority) return a.priority - b.priority;
    const da = difficultyOrder(a.difficulty);
    const db = difficultyOrder(b.difficulty);
    if (da !== db) return da - db;
    return a.title.localeCompare(b.title, "en");
});
---

<BaseLayout {title} {description}>
    <!-- Hero Section -->
    <header class="relative mb-12 overflow-hidden rounded-3xl bg-gradient-to-br from-emerald-600 via-emerald-700 to-teal-800 p-8 text-white shadow-xl sm:p-12">
        <div class="absolute inset-0 opacity-10">
            <svg class="h-full w-full" viewBox="0 0 100 100" preserveAspectRatio="none">
                <defs>
                    <pattern id="waves" width="20" height="20" patternUnits="userSpaceOnUse">
                        <path d="M0 10 Q5 5, 10 10 T20 10" fill="none" stroke="white" stroke-width="0.5"/>
                    </pattern>
                </defs>
                <rect width="100" height="100" fill="url(#waves)" />
            </svg>
        </div>
        <div class="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-white/10 blur-3xl"></div>
        <div class="absolute -bottom-32 -left-32 h-80 w-80 rounded-full bg-emerald-400/20 blur-3xl"></div>

        <div class="relative">
            <div class="inline-flex items-center gap-2 rounded-full bg-white/20 px-3 py-1 text-xs font-medium backdrop-blur-sm">
                <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                </svg>
                {articles.length} Articles
            </div>

            <h1 class="mt-4 text-3xl font-bold tracking-tight sm:text-4xl md:text-5xl">
                Training Knowledge
            </h1>
            <p class="mt-4 max-w-2xl text-lg text-emerald-100">
                Practical, science-backed training advice for cyclists with real lives.
                No pro-level volume, no complicated periodization – just what works for regular people who want to get faster.
            </p>

            <div class="mt-6 flex flex-wrap gap-3 text-sm">
                <span class="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-1 backdrop-blur-sm">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                    Evidence-based
                </span>
                <span class="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-1 backdrop-blur-sm">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                    Time-efficient
                </span>
                <span class="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-1 backdrop-blur-sm">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" /></svg>
                    For recreational cyclists
                </span>
            </div>
        </div>
    </header>

    <!-- Quick CTA -->
    <div class="mb-10 rounded-2xl border border-emerald-200 bg-emerald-50 p-5 text-sm text-emerald-900">
        <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <p class="font-semibold">Start here: Calculate your training zones</p>
                <p class="text-emerald-800">Get your FTP and dial in your zones before you follow a plan.</p>
            </div>
            <Button href="/tools/ftp-calculator" variant="subtle">Open FTP Calculator →</Button>
        </div>
    </div>

    {articles.length > 0 ? (
        <>
            <!-- Featured Article (first one) -->
            {articles.length > 0 && (
                <a href={articles[0].url} class="group relative mb-8 block overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition-all hover:shadow-xl hover:-translate-y-1">
                    <div class="absolute inset-0 bg-gradient-to-r from-emerald-500/5 to-teal-500/5"></div>
                    <div class="relative flex flex-col md:flex-row">
                        <div class="flex-1 p-6 sm:p-8">
                            <div class="flex items-center gap-3">
                                <span class={`inline-flex items-center gap-1 rounded-full bg-gradient-to-r ${articles[0].colors.bg} px-2.5 py-0.5 text-xs font-medium text-white`}>
                                    ⭐ Featured
                                </span>
                                {articles[0].difficulty && (
                                    <span class="rounded-full bg-slate-100 px-2.5 py-0.5 text-xs font-medium text-slate-600 dark:bg-slate-600 dark:text-white dark:border dark:border-slate-400/40">
                                        {articles[0].difficulty.charAt(0).toUpperCase() + articles[0].difficulty.slice(1)}
                                    </span>
                                )}
                            </div>

                            <h2 class="mt-4 text-2xl font-bold text-slate-900 group-hover:text-emerald-600 transition-colors sm:text-3xl">
                                {articles[0].title}
                            </h2>

                            <p class="mt-3 text-sm text-slate-600 leading-relaxed">
                                {articles[0].description}
                            </p>

                            <div class="mt-4 flex flex-wrap gap-2">
                                {articles[0].tags.map((tag) => (
                                    <span class="rounded-full bg-slate-100 px-2.5 py-0.5 text-xs font-medium text-slate-600 dark:bg-slate-600 dark:text-white dark:border dark:border-slate-400/40">
                                        {tag}
                                    </span>
                                ))}
                            </div>

                            <div class="mt-6 flex items-center gap-4 text-xs text-slate-500">
                                {articles[0].readingMinutes && (
                                    <span class="flex items-center gap-1">
                                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                        </svg>
                                        {articles[0].readingMinutes} min read
                                    </span>
                                )}
                                {articles[0].updatedLabel && (
                                    <span class="flex items-center gap-1">
                                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                        </svg>
                                        {articles[0].updatedLabel}
                                    </span>
                                )}
                            </div>

                            <div class="mt-6 inline-flex items-center text-sm font-medium text-emerald-600 group-hover:text-emerald-700">
                                Read full article
                                <svg class="ml-1 h-4 w-4 transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                                </svg>
                            </div>
                        </div>

                        <div class="hidden md:flex items-center justify-center bg-gradient-to-br from-emerald-50 to-teal-50 p-8 md:w-80">
                            <div class="text-8xl">🚴‍♂️</div>
                        </div>
                    </div>
                </a>
            )}

            <!-- Other Articles Grid -->
            {articles.length > 1 && (
                <div class="grid gap-6 md:grid-cols-2">
                    {articles.slice(1).map((article) => (
                        <a href={article.url} class={`group relative overflow-hidden rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition-all hover:shadow-lg ${article.colors.border} hover:-translate-y-1`}>
                            <div class={`absolute right-0 top-0 h-32 w-32 translate-x-8 -translate-y-8 rounded-full bg-gradient-to-br ${article.colors.light} opacity-50 transition-transform group-hover:scale-150`}></div>

                            <div class="relative">
                                <div class="flex items-center justify-between">
                                    <div class={`inline-flex h-10 w-10 items-center justify-center rounded-xl bg-gradient-to-br ${article.colors.bg} text-white shadow-lg ${article.colors.shadow}`}>
                                        <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                                        </svg>
                                    </div>

                                    {article.difficulty && (
                                        <span class={`rounded-full px-2.5 py-0.5 text-xs font-medium ${article.colors.text} bg-slate-100 dark:bg-slate-600 dark:text-white dark:border dark:border-slate-400/40`}>
                                            {article.difficulty.charAt(0).toUpperCase() + article.difficulty.slice(1)}
                                        </span>
                                    )}
                                </div>

                                <h2 class={`mt-4 text-lg font-semibold text-slate-900 group-hover:${article.colors.text} transition-colors`}>
                                    {article.title}
                                </h2>

                                <p class="mt-2 text-sm text-slate-600 leading-relaxed line-clamp-2">
                                    {article.description}
                                </p>

                                <div class="mt-3 flex flex-wrap gap-1.5">
                                    {article.tags.slice(0, 3).map((tag) => (
                                        <span class="rounded-full bg-slate-100 px-2 py-0.5 text-[0.65rem] font-medium text-slate-600 dark:bg-slate-600 dark:text-white dark:border dark:border-slate-400/40">
                                            {tag}
                                        </span>
                                    ))}
                                </div>

                                <div class="mt-4 flex items-center justify-between text-xs text-slate-500">
                                    <div class="flex items-center gap-3">
                                        {article.readingMinutes && (
                                            <span class="flex items-center gap-1">
                                                <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                                </svg>
                                                {article.readingMinutes} min
                                            </span>
                                        )}
                                    </div>

                                    <span class={`inline-flex items-center text-xs font-medium ${article.colors.text}`}>
                                        Read
                                        <svg class="ml-1 h-3.5 w-3.5 transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                                        </svg>
                                    </span>
                                </div>
                            </div>
                        </a>
                    ))}
                </div>
            )}
        </>
    ) : (
        <div class="rounded-2xl border border-slate-200 bg-white p-8 text-center">
            <div class="text-4xl mb-4">📝</div>
            <p class="text-slate-600">No articles published yet. Coming soon!</p>
        </div>
    )}

    <!-- Bottom CTA -->
    <div class="mt-12 rounded-2xl border border-slate-200 bg-gradient-to-r from-slate-50 to-slate-100 p-6 text-center sm:p-8 dark:border-slate-800 dark:from-slate-900 dark:to-slate-800">
        <h2 class="text-lg font-semibold text-slate-900 dark:text-slate-100">Ready to put theory into practice?</h2>
        <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">
            Use our calculators to set up your training zones and plan your nutrition.
        </p>
        <div class="mt-4">
            <Button href="/tools" variant="ghost">
                Explore cycling tools →
            </Button>
        </div>
    </div>
</BaseLayout>
