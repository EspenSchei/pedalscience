---
import "../styles/global.css";
import Breadcrumb from "../components/Breadcrumb.astro";
import Button from "../components/Button.astro";

const {
    title = "Road Cycling",
    description = "Tools and knowledge for road cyclists",
} = Astro.props;

const year = new Date().getFullYear();
const pathname = Astro.url.pathname;

/* -------------------------------------------------------
   BREADCRUMB LOGIC
------------------------------------------------------- */

let breadcrumbItems = null;
let isToolDetail = false;

if (pathname.startsWith("/tools/") && pathname !== "/tools") {
    isToolDetail = true;
    breadcrumbItems = [
        { label: "Home", href: "/" },
        { label: "Tools", href: "/tools" },
        { label: title },
    ];
}

if (pathname.startsWith("/training/") && pathname !== "/training") {
    breadcrumbItems = [
        { label: "Home", href: "/" },
        { label: "Training", href: "/training" },
        { label: title },
    ];
}

/* -------------------------------------------------------
   SEO: Schema.org BreadcrumbList
------------------------------------------------------- */

let breadcrumbJson = null;

if (breadcrumbItems) {
    const origin = Astro.url.origin ?? "";
    breadcrumbJson = {
        "@context": "https://schema.org",
        "@type": "BreadcrumbList",
        itemListElement: breadcrumbItems.map((item, index) => ({
            "@type": "ListItem",
            position: index + 1,
            name: item.label,
            item: item.href ? `${origin}${item.href}` : Astro.url.href,
        })),
    };
}
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="utf-8" />
    <title>{title} | Road Cycling</title>
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    {breadcrumbJson && (
        <script type="application/ld+json">
        {JSON.stringify(breadcrumbJson)}
      </script>
    )}
</head>

<body class="min-h-screen bg-slate-50 text-slate-900">
<!-- HEADER / NAV -->
<header
        class="sticky top-0 z-20 border-b border-slate-200 bg-white/90 backdrop-blur shadow-sm"
>
    <div
            class="mx-auto flex max-w-5xl items-center justify-between px-4 py-3 sm:px-6"
    >
        <a
                href="/"
                class="inline-flex items-center rounded-full border border-slate-300 bg-white px-3 py-1 text-xs font-semibold uppercase tracking-[0.2em] text-slate-700"
        >
            Road Cycling
        </a>

        <nav class="flex gap-2 text-sm">
            <a
                    href="/"
                    class="rounded-full px-3 py-1 text-slate-600 hover:bg-slate-100 hover:text-slate-900"
            >
                Home
            </a>
            <a
                    href="/tools"
                    class="rounded-full px-3 py-1 text-slate-600 hover:bg-slate-100 hover:text-slate-900"
            >
                Tools
            </a>
            <a
                    href="/training"
                    class="rounded-full px-3 py-1 text-slate-600 hover:bg-slate-100 hover:text-slate-900"
            >
                Training
            </a>
        </nav>
    </div>
</header>

<!-- CONTENT -->
<main class="py-8">
    <div class="mx-auto max-w-5xl px-4 sm:px-6">
        {breadcrumbItems && (
                <>
                    <Breadcrumb items={breadcrumbItems} />
                    {isToolDetail && (
                            <div class="mb-4">
                                <Button
                                        href="/tools"
                                        variant="ghost"
                                        class="px-3 py-1 text-xs"
                                >
                                    ← Back to tools
                                </Button>
                            </div>
                    )}
                </>
        )}

        <slot />
    </div>
</main>

<!-- FOOTER -->
<footer
        class="border-t border-slate-200 bg-slate-50 text-xs text-slate-500"
>
    <div class="mx-auto max-w-5xl px-4 py-4 sm:px-6">
        © {year} Road Cycling – for recreational cyclists and nerds 🚴‍♂️
    </div>
</footer>
</body>
</html>